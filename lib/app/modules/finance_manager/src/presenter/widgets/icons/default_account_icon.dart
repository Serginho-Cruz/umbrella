import 'package:flutter/material.dart';

class DefaultAccountIcon extends StatelessWidget {
  const DefaultAccountIcon({super.key});

  @override
  Widget build(BuildContext context) {
    return const Icon(Icons.star_outlined, size: 48);
  }
}
